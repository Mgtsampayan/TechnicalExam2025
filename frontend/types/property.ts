export interface Property {
    id: string;
    type: string;
    model: string;
    location: string;
    area: string;
    price: string;
    availability: string;
    imageUrl?: string;
    mapUrl?: string;
    description?: string;
}